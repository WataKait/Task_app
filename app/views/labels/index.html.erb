<%= stylesheet_link_tag "labels" %>

<%= flash[:notice] if flash[:notice].present? %>

<h1><%= t :label_list %></h1>

<p><%= link_to t(:transition_label_create), new_label_path %></p>

<table>
  <thead>
    <tr>
      <th><%= t :label %></th>
    </tr>
  </thead>
  <tbody>
    <% @labels.each do |label| %>
      <tr>
        <td><%= label.name %></td>
      </tr>
    <% end %>
  </tbody>
</table>
